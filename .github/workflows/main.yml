name: 微博监控

on:
  schedule:
    - cron: '0 23 * * *'  # 7点北京
    - cron: '0 0 * * *'   # 8点北京
    - cron: '0 1 * * *'   # 9点北京
    - cron: '0 2 * * *'   # 10点北京
    - cron: '0 4 * * *'   # 12点北京
    - cron: '0 7 * * *'   # 15点北京
    - cron: '0 8 * * *'   # 16点北京
    - cron: '0 9 * * *'   # 17点北京
    - cron: '0 10 * * *'  # 18点北京
    - cron: '0 11 * * *'  # 19点北京
    - cron: '0 12 * * *'  # 20点北京
    - cron: '0 13 * * *'  # 21点北京
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests python-dateutil pytz lark-oapi
      - run: python weibo.py
      - name: 提交状态文件
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add last_processed_id.txt
          git commit -m "Update status $(date '+%Y-%m-%d %H:%M')" || echo "无需提交"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} HEAD:main || echo "无需推送"
      - if: ${{ always() }}
        run: |
          if [ -n "$NEW_SUB_COOKIE" ]; then
            echo "$NEW_SUB_COOKIE" | gh secret set WEIBO_SUB_COOKIE -b - --repo "${{ github.repository }}"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
